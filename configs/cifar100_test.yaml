# CIFAR-100 test run — quick validation before full experiments
dataset: cifar100
model: resnet
results_dir: results_test_scratch/cifar100
clients_per_round: [5]
num_clients: 100
num_rounds: 5
alpha: 0.5
beta: 0.3
reward_formula: full
use_target_network: false
save_checkpoints: false
dirichlet_alpha: 0.5
